// This file is deprecated. The Base64 font string caused issues with PDF generation size and stability.
// The application now uses the html2canvas approach in `lib/pdfExport.ts`, which relies on system/browser fonts.
// This constant is left empty to prevent breaking any potential imports, but it should not be used.
export const vazirmatnFont = ``;
